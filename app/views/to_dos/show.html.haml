%h1 Todo

%p
  %label Description:
  = @to_do.description

%p
  %label Due:
  = @to_do.due_on

%p
  = link_to "Edit Todo", edit_to_do_path(@to_do)
  = link_to "Remove Todo", to_do_path(@to_do), :method => :delete, :confirm => "Are you sure?"
  = link_to "Back to Todo list", to_dos_path
